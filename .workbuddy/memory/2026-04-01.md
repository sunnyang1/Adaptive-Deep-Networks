# 2026-04-01 Daily Log

## TurboQuant → RaBitQ 全量替换完成

将整个项目中的 TurboQuant V3 重命名为 RaBitQ (Rapid and Accurate Bit-level Quantization)。

### 核心变更
- `src/turboquant/` → `src/rabitq/` (新建，旧目录保留为 legacy)
- 类名映射: `TurboQuantV3` → `RaBitQ`, `TurboQuantConfig` → `RaBitQConfig`, `V3Cache` → `RaBitQCache`
- `__init__.py` 添加了 `__getattr__` 对 legacy import 给出清晰错误提示

### 源码更新
- `src/gating/depth_priority.py` — 注释和文档字符串中 TurboQuant → RaBitQ
- `src/gating/__init__.py` — 注释更新
- `src/qttt/polar_adaptation.py` — 注释和 `compute_effective_cost()` 返回键 `turboquant_discount` → `rabitq_discount`
- `src/attnres/polar_pseudo_query.py` — 注释更新

### 测试/脚本更新
- `tests/test_turboquant_v3_refactored.py` → `tests/test_rabitq_refactored.py`
- `tests/unit/test_turboquant_v3.py` → `tests/unit/test_rabitq.py`
- `scripts/validate_turboquant_v3.py` → `scripts/validate_rabitq.py`
- 修复 `MSECompressor.fit()`/`compress()` 缺少 `head_dim` 参数
- 放宽质量测试阈值 (4-bit/2-bit 随机数据误差较大)
- 5 个使用旧 API 的脚本移至 `scripts/experiments/legacy/` 和 `experiments/validation/legacy/`

### 文档/配置更新
- `README.md` — 所有 TurboQuant 引用更新
- `pyproject.toml` — description 更新
- `configs/experiments/validation_targets.yaml` — `turboquant:` → `rabitq:`

### 验证结果
- 全局 grep 确认活跃代码中零 TurboQuant 残留 (排除 legacy 目录)
- `pytest tests/unit/test_rabitq.py tests/test_rabitq_refactored.py -v` → **50 passed, 0 failed**
